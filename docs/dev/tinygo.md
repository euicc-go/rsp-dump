# Using `TinyGo`?

[PR]: https://github.com/tinygo-org/tinygo/pull/4376

`TinyGo` has not fully implemented [`Type.AssignableTo`](PR), which makes `encoding/json` unusable. As a result, this project cannot run, but it can currently compile.